import styled from "styled-components";
import SelectTheme from "./SelectTheme";

const SelectOption = styled.option`
  margin: 0;
  padding: 0;
  outline: 0;
`;

/* eslint-disable */
const SelectOptionStyle = styled(SelectOption)`

`;

export default SelectOptionStyle;
